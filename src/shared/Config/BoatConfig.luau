--!strict
-- Shipment limits and economy defaults. Per-type boat stats removed — use `ShipStats` + `ShipSkinCatalog`.

local BoatConfig = {
	MaxConcurrentShipmentsPerPlayer = 30,
}

return BoatConfig
